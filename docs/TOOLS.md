# Tools/Toolchains

__About the binary tools and MinGW toolchain used by this repo.__

 - GN-Legacy includes the tools `gn`, `ninja`, `wget`, and downloads MinGW toolchains in multiple OS/Arch flavours.

## GN and Ninja

GN is a "meta-build system", that stands for "Generate Ninja". It is highly configurable, hierachal, and uses *.gn*
and *.gni* (gn Include) files as its format. All of the BUILD.gn and .gni files gn-legacy uses for configuring itself
and setting target compiler flags is in the [build](../build/) dir. GN parses your arguments "args" in your *args.gn*
file located in the build output dir, and generates *.ninja* files that Ninja itself then consumes.

Ninja is the true build system; it reads the __.ninja__ files generated by GN in an output directory, and uses those to spawn instances
of the compiler, appending the appropriate flags, and pointing at your sources. It is what ultimately drops your finished executables/libraries
in __out/__.

I maintain forks of each, both supporting Windows XP+, see [gn-xp](https://github.com/Alex313031/gn-xp#readme) and [ninja-xp](https://github.com/Alex313031/ninja-xp#readme).

Both of these binaries are downloaded for your host platform by [download_toolchains.sh](../tools/download_toolchains.sh), and used by the gn/gn.bat
and ninja/ninja.bat wrappers in the root of the repo.

## About the Compiler Toolchain

gn-legacy uses a custom [MinGW](https://www.mingw-w64.org/) toolchain + [patches](https://github.com/Alex313031/mingw-build/tree/master/patches)
and compiler configs to allow targeting all the way back to Windows NT 4.0.  

The toolchain is built from [my fork](https://github.com/Alex313031/mingw-build) of [mingw-w64-build](https://github.com/Zeranoe/mingw-w64-build).  
There are [GCC](https://gcc.gnu.org/) and [LLVM](https://llvm.org/) flavors too; you can control which is used to build your projects via the gn arg `use_llvm`.

On the LLVM toolchain, you can also set `use_clang_cl = true` to compile with the MSVC-style [clang-cl](https://clang.llvm.org/docs/UsersManual.html#clang-cl) driver
instead of the default GCC-style driver. It still targets the MinGW runtime (so linking and the standard library are unchanged), and the compiler flags in
[build/config](../build/config/) are translated to their MSVC spellings where applicable. Pair it with `use_pdbs = true` to emit CodeView debug info and standalone
`.pdb` files next to your binaries. Both args are Windows + LLVM only.

## Resources

See the:  

[GCC Manual](https://gcc.gnu.org/onlinedocs/gcc-16.1.0/gcc/) and [x86 SIMD options](https://gcc.gnu.org/onlinedocs/gcc-16.1.0/gcc/x86-Options.html)

[LLVM Clang Command Line Reference](https://clang.llvm.org/docs/ClangCommandLineReference.html)

[LLVM Clang User Manual](https://clang.llvm.org/docs/UsersManual.html)

[LLVM LLD Manual](https://lld.llvm.org/)
